name: provider_feature_first
description: Test fixture for Provider feature-first architecture.
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  dio: ^5.7.0
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
