name: dio_in_ui
description: Doctor fixture with direct Dio in UI.
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
